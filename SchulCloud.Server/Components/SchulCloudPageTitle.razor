@using Microsoft.Extensions.Options
@using SchulCloud.Server.Options

@inject IOptions<PresentationOptions> PresentationOptions

<PageTitle>@Title - @PresentationOptions.Value.ApplicationName</PageTitle>

@code {
    [Parameter]
    public required string Title { get; set; }
}
