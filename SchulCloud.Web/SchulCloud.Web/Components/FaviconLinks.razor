@using SchulCloud.Web.Options;

@foreach (PresentationOptions.Favicon favicon in PresentationOptionsAccessor.Value.Favicons)
{
    <link rel="icon" href="@favicon.Path" type="@favicon.MimeType" sizes="@favicon.Sizes" />
}