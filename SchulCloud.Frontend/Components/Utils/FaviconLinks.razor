@using SchulCloud.Frontend.Options;

@foreach (PresentationOptions.Favicon favicon in PresentationOptionsAccessor.Value.Favicons)
{
    <link rel="icon" href="@Assets[favicon.Path]" type="@favicon.MimeType" sizes="@favicon.Sizes" />
}